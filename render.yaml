services:
  - type: web
    name: daily-haraka-webhook
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: FIREBASE_CREDENTIALS
        sync: false
